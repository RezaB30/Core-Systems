@model RadiusR_Manager.Models.ViewModels.ForgotPasswordConfirmViewModel

@{
    ViewBag.Title = RadiusR.Localization.Pages.Common.ForgotPassword;
    Layout = "~/Views/Shared/_NoAuthLayout.cshtml";
}

<div style="text-align: center;">
    <div class="login-form">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.Hidden("Email", Session["change_password_email"])
            <table class="input-table">
                <tr>
                    <td>
                        @Html.LabelFor(model => model.SMSCode)
                    </td>
                    <td>
                        @Html.EditorFor(model => model.SMSCode, new { @htmlAttributes = new { @autocomplete = "off", @maxlength = 150 } })
                    </td>
                </tr>
                <tr>
                    <td colspan="2">
                        @Html.ValidationMessageFor(model => model.SMSCode)
                    </td>
                </tr>
                <tr>
                    <td>
                        @Html.LabelFor(model => model.NewPassword)
                    </td>
                    <td>
                        @Html.EditorFor(model => model.NewPassword, new { @htmlAttributes = new { @autocomplete = "off", @maxlength = 100 } })
                    </td>
                </tr>
                <tr>
                    <td colspan="2">
                        @Html.ValidationMessageFor(model => model.NewPassword)
                    </td>
                </tr>
                <tr>
                    <td colspan="2">
                        @Html.ValidationMessage("General")
                    </td>
                </tr>
                <tr>
                    <td style="text-align: left;">
                        @Html.ActionLink(RadiusR.Localization.Pages.Common.Back, "Login", null, new { @class = "link-button iconed-button back-button" })
                    </td>
                    <td style="text-align: right;">
                        <input type="submit" value="@RadiusR.Localization.Pages.Common.Save" class="link-button iconed-button accept-button" />
                    </td>
                </tr>
            </table>
        }
    </div>
</div>

